# App icon placeholder

No production icon yet. Before TestFlight:

1. Produce a 1024x1024 PNG master icon (no transparency for iOS).
2. Generate platform icon sets, e.g. with the `flutter_launcher_icons`
   package (add it under dev_dependencies, configure, run
   `dart run flutter_launcher_icons`).
3. Verify `ios/Runner/Assets.xcassets/AppIcon.appiconset` is filled in.

Suggested look: dark background (#0B0E13), teal accent (#4FD1C5),
minimal geometric mark. Serious, calm, technical.
